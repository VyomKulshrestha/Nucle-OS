# Project Tree Fixture

A small, metadata-heavy directory tree used to exercise multi-file storage
(varied filenames, sizes, and nesting depth) as a single benchmark/regression
unit, rather than one file at a time.
