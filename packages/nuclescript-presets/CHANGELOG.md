# Changelog

## 0.1.0

- Initial `@nuclescript/presets` package release.
- Added `medical_archive`, `twist_archive`, `reliable_store`, and
  `illumina_recovery` exports.
- Added package metadata for registry indexing and compiler validation.
