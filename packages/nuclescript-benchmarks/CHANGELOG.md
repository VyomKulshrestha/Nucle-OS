# Changelog

## 0.1.0

- Initial `@nuclescript/benchmarks` package release.
- Added `benchmark_text_archive`, `benchmark_binary_archive`,
  `benchmark_fasta_archive`, and `benchmark_image_archive` pool schema exports.
- Added `small_text_workload`, `binary_archive_workload`, `fasta_workload`,
  and `image_workload` pipeline exports.
